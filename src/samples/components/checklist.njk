---
layout: layouts/base.njk
title: Checklist
headerType: doc
asideType: samples
---
{% import "macros/ui.njk" as ui %}

<section class="portal__section">
    <h1 class="portal__section-title">Checklist</h1>
    <p class="portal__introduction-text">
      O componente de checklist é usado na barra lateral dos módulos para que os alunos possam rastrear seu progresso em cada aula.
    </p>

    <hr class="portal__divider">

    <!-- 1. SHOWCASE -->
    <h2 class="portal__section-title"><i class="fa-solid fa-eye"></i> Exemplo Visual</h2>

    {% set items = [
      { id: 'sample-1', label: 'Item de exemplo 1', completed: true },
      { id: 'sample-2', label: 'Item de exemplo 2' },
      { id: 'sample-3', label: 'Item de exemplo 3' }
    ] %}
    {{ ui.checklist(items) }}

    <hr class="portal__divider">

    <!-- 2. IMPLEMENTATION -->
    <h2 class="portal__section-title"><i class="fa-solid fa-code"></i> Como Utilizar</h2>

    <h3 class="portal__section-title">Código</h3>
    {{ ui.codeBlock(
      language='markdown',
      code='{% import "macros/ui.njk" as ui %}\n\n{% set checklistItems = [\n  { id: "task-1", label: "Ler a documentação", completed: true },\n  { id: "task-2", label: "Praticar os comandos" },\n  { id: "task-3", label: "Realizar o desafio" }\n] %}\n\n{{ ui.checklist(checklistItems) }}'
    ) }}

    <h3 class="portal__section-title">Parâmetros</h3>
    <ul class="portal__features-list">
        <li><strong>items</strong> (obrigatório): Array de objetos contendo:
            <ul>
                <li><strong>id</strong>: Identificador único do item</li>
                <li><strong>label</strong>: Texto do item</li>
                <li><strong>completed</strong>: Booleano indicando se inicia marcado (opcional)</li>
            </ul>
        </li>
    </ul>
</section>

{% block scripts %}
<script>
  // Script simplificado para funcionamento do exemplo
  function updateChecklistExample() {
    const checklist = document.querySelectorAll('.module-checklist__item input[type="checkbox"]');
    if (checklist.length === 0) return;
    
    const checked = document.querySelectorAll('.module-checklist__item input[type="checkbox"]:checked').length;
    const checklistCount = document.getElementById('checklist-count');
    
    if (checklistCount) checklistCount.textContent = checked;
  }

  document.addEventListener('DOMContentLoaded', function() {
    updateChecklistExample();
    document.querySelectorAll('.module-checklist__item input[type="checkbox"]').forEach(cb => {
      cb.addEventListener('change', updateChecklistExample);
    });
  });
</script>
{% endblock %}
